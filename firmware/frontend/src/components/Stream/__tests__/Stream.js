import React from 'react'
import { render, screen } from '@testing-library/react'
import Stream from '../index'
import SignalProvider from "../../SignalProvider";

it("Renders correctly", () => {
    /*
    render(
        <SignalProvider url={test}>
            <Stream />
        </SignalProvider>
    )
     */
})

